node_modules/
*.log
.DS_Store

# Machine-local registry now lives in ~/.boss/registry.json (not in the repo).
# Ignore any stray legacy copy so local project paths never get committed again.
registry/projects.json

# Business/pricing strategy — kept local by choice (not for public eyes yet).
# Publish deliberately later if/when it should be open.
docs/business/
