# L2 · V1 — NOT YET AUTHORED

The shippable-v1 mode — "ready for a real, polished release." Planned contents (generalized from dhun):

- design tokens + `/design-review` + `/ux-check`
- prototype process + REGISTRY
- `/board` (live working state from frontmatter)
- `AGENT_DOC_MAP` doc-placement contract
- `/contract-check` (type/interface sync)
- agents: `ui-designer`, `ux-designer`, `db-architect`, `docs-writer`, `prototype-builder`

To author: add `manifest.json` + `template/` here.
