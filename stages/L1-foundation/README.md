# L1 · Foundation — NOT YET AUTHORED

The MVP-spine layer. Planned contents (generalized from dhun):

- `/spec` skill + `FEAT-NNN` numbering
- `/smoke` build-health gate (stack-configured)
- devlog + `/log`
- `/close` + `RESUME.md` convention
- session-start / session-end hooks
- write-safety + permissions hardening
- agents: `tester`, `program-manager`

To author: add `manifest.json` + `template/` here (mirror `stages/L0-sketch/`).
Until then, `boss unlock L1` reports "not authored yet" gracefully.
