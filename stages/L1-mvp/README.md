# L1 · MVP — NOT YET AUTHORED

The MVP-spine mode — "ready to build the first working version." Planned contents (generalized from dhun):

- `/spec` skill + `FEAT-NNN` numbering
- `/smoke` build-health gate (stack-configured)
- devlog + `/log`
- `/close` + `RESUME.md` convention
- session-start / session-end hooks
- write-safety + permissions hardening
- agents: `tester`, `program-manager`

To author: add `manifest.json` + `template/` here (mirror `stages/L0-quickstart/`).
Until then, `boss unlock mvp` reports "not authored yet" gracefully.
